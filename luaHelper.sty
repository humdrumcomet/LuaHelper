\ProvidesPackage{luaHelper}
\RequirePackage{luacode}
\RequirePackage{siunitx}
\directlua{
    path= "\packagePath"
    import = require(path.."/luaHelper/luaHelper")
}
\newcommand\luVal[1]{%
    \directlua{tex.sprint(#1)}%
}
\newcommand\siluVal[3][scaler]{%
    \SI{\directlua{printNum(#2,#1)}}{#3}%
}
\newcommand\printLuaTable[][opts]{
}
